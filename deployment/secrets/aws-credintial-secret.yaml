# apiVersion: v1
# kind: Secret
# metadata:
#   name: aws-secret
#   namespace: ethereum
# type: Opaque
# data:
#   access-key: YWJjZA==  # Base64-encoded value of "abcd"
#   secret-access-key: ZGVmZw==  # Base64-encoded value of "defg"

#----------------------------Manualy do it once in instance-----------------------------------------#

# install helm if not exist, also exernal secret repo...
#1-->helm repo add external-secrets https://charts.external-secrets.io
#2-->helm repo update


#Install ESO in external-secrets namespace...
#1-->helm upgrade --namespace external-secrets --create-namespace --install --wait external-secrets external-secrets/external-secrets

# For verify ESO installation...
# kubectl -n external-secrets get all


# echo -n "REPLACE_ME_WITH_YOUR_ACCESS_KEY" > access-key
# echo -n "REPLACE_ME_WITH_YOUR_SECRET_KEY" > secret-access-key
#kubectl -n ethereum create secret generic aws-secret --from-file=./access-key --from-file=./secret-access-key